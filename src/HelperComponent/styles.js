import { css } from 'emotion';

const themeSelector = css`
  position: sticky;
  top: 40px;
  display: flex;
  justify-content: flex-end;
  max-width: 900px;
  padding-left: 10px;
  padding-right: 10px;
  margin: 0 auto;
`;

export default {
  themeSelector
};